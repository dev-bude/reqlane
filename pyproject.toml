[project]
name = "reqlane"
dynamic = ["version"]
description = "Requests between AI coding agents that own different repositories, with consumer acceptance and owner escalation"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.110", "uvicorn>=0.29", "typer>=0.12", "httpx>=0.27"]
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
readme = "README.md"
keywords = ["ai-agents", "claude-code", "codex", "multi-agent", "request-lifecycle"]
authors = [{ name = "SimeonAr" }]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Environment :: Console",
    "Framework :: FastAPI",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Version Control",
]

[project.urls]
Homepage = "https://github.com/dev-bude/reqlane"
Repository = "https://github.com/dev-bude/reqlane"
Issues = "https://github.com/dev-bude/reqlane/issues"
Protocol = "https://github.com/dev-bude/reqlane/blob/main/PROTOCOL.md"
Security = "https://github.com/dev-bude/reqlane/blob/main/SECURITY.md"

[project.optional-dependencies]
dev = ["pytest>=8"]

[project.scripts]
reqlane = "reqlane.cli.main:app"

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[tool.setuptools.dynamic]
version = {attr = "reqlane.__version__"}

[tool.setuptools.packages.find]
include = ["reqlane*"]

[tool.setuptools.package-data]
reqlane = ["server/ui.html"]

[tool.pytest.ini_options]
testpaths = ["tests"]
